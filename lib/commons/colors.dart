import 'package:flutter/cupertino.dart';

class MyColors {
  // ============================================================
  // BRAND
  // ============================================================

  /// Warna utama aplikasi — sage green
  static const Color primary = Color(0xFF7F9A5A);

  /// Primary yang lebih terang untuk surface / selected state
  static const Color primaryLight = Color(0xFFE8EEDF);

  /// Primary yang lebih gelap untuk pressed / active
  static const Color primaryDark = Color(0xFF627A43);

  // ============================================================
  // ACCENT
  // ============================================================

  /// Beige / warm neutral
  static const Color secondary = Color(0xFFD8CDB8);

  /// Merah muted — error / delete
  static const Color red = Color(0xFFC95C5C);

  /// Biru muted — info
  static const Color blue = Color(0xFF5B86A5);

  /// Amber muted — warning
  static const Color yellow = Color(0xFFD6A84F);

  /// Grey
  static const Color grey = Color(0xFF9A9A94);

  // ============================================================
  // TEXT
  // ============================================================

  /// Jangan pakai hitam pekat agar UI lebih soft
  static const Color textDark = Color(0xFF252823);

  static const Color textPrimary = Color(0xFF252823);

  static const Color textSecondary = Color(0xFF73776E);

  static const Color textMuted = Color(0xFFA0A39C);

  static const Color textOnPrimary = Color(0xFFFFFFFF);

  // ============================================================
  // BACKGROUND
  // ============================================================

  /// Background utama aplikasi
  static const Color background = Color(0xFFF8F8F5);

  /// Surface/card
  static const Color surface = Color(0xFFFFFFFF);

  /// Surface sedikit berbeda dari background
  static const Color surfaceSoft = Color(0xFFF2F3ED);

  /// Border
  static const Color border = Color(0xFFE3E5DE);

  /// Divider
  static const Color divider = Color(0xFFE9EAE5);

  // ============================================================
  // STATUS
  // ============================================================

  static const Color success = Color(0xFF6F9460);

  static const Color successBg = Color(0xFFEAF2E6);

  static const Color warning = Color(0xFFC49A4A);

  static const Color warningBg = Color(0xFFF8F1DF);

  static const Color error = Color(0xFFC95C5C);

  static const Color errorBg = Color(0xFFF8E9E9);

  static const Color info = Color(0xFF5B86A5);

  static const Color infoBg = Color(0xFFEAF1F5);

  // ============================================================
  // NOTION / CATEGORY BACKGROUNDS
  // ============================================================

  static const Color notionBgGrey = Color(0xFFF1F1EF);

  static const Color notionBgBrown = Color(0xFFF4EEEE);

  static const Color notionBgOrange = Color(0xFFFAEBDD);

  static const Color notionBgYellow = Color(0xFFFBF3DB);

  static const Color notionBgGreen = Color(0xFFEDF3EC);

  static const Color notionBgBlue = Color(0xFFE7F3F8);

  static const Color notionBgPurple = Color(0xFFF6F3F9);

  static const Color notionBgPink = Color(0xFFFAF1F5);

  static const Color notionBgRed = Color(0xFFFDEBEC);

  // ============================================================
  // OVERLAY
  // ============================================================

  static const Color overlay = Color(0x14000000);

  static const Color shadow = Color(0x12000000);
}
