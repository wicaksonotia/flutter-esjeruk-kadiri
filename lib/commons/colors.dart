import 'package:flutter/cupertino.dart';

class MyColors {
  // ============================================================
  // BRAND — NAVY
  // ============================================================

  /// Brand utama.
  /// Digunakan untuk CTA, primary action, icon brand,
  /// accent section, dan elemen aktif.
  static const Color primary = Color(0xFF405D73);

  /// Soft navy background.
  /// Digunakan untuk selected state dan subtle highlight.
  static const Color primaryLight = Color(0xFFE8EFF4);

  /// Deep navy.
  /// Digunakan untuk text/icon aktif dan pressed state.
  static const Color primaryDark = Color(0xFF2F485C);

  // ============================================================
  // NEUTRAL
  // ============================================================

  static const Color textDark = Color(0xFF252A2F);

  static const Color textPrimary = Color(0xFF252A2F);

  static const Color textSecondary = Color(0xFF70777E);

  static const Color textMuted = Color(0xFFA0A6AB);

  static const Color textOnPrimary = Color(0xFFFFFFFF);

  // ============================================================
  // BACKGROUND
  // ============================================================

  /// Main application background
  static const Color background = Color(0xFFF8F9FA);

  /// Card / modal / input
  static const Color surface = Color(0xFFFFFFFF);

  /// Secondary surface
  static const Color surfaceSoft = Color(0xFFF1F4F6);

  /// Border
  static const Color border = Color(0xFFDDE3E8);

  /// Divider
  static const Color divider = Color(0xFFE7EBEE);

  // ============================================================
  // STATUS
  // ============================================================

  static const Color success = Color(0xFF648A72);

  static const Color successBg = Color(0xFFEAF1EC);

  static const Color warning = Color(0xFFC69B58);

  static const Color warningBg = Color(0xFFF8F1E3);

  static const Color error = Color(0xFFC86B69);

  static const Color errorBg = Color(0xFFF9EAEA);

  static const Color info = Color(0xFF607F98);

  static const Color infoBg = Color(0xFFEAF0F4);

  // ============================================================
  // CATEGORY / SOFT BACKGROUNDS
  // ============================================================

  static const Color notionBgGrey = Color(0xFFF1F3F5);

  static const Color notionBgBrown = Color(0xFFF4EFEB);

  static const Color notionBgOrange = Color(0xFFFAECDE);

  static const Color notionBgYellow = Color(0xFFFBF3DD);

  static const Color notionBgGreen = Color(0xFFEDF3E9);

  static const Color notionBgBlue = Color(0xFFE8F0F5);

  static const Color notionBgPurple = Color(0xFFF2EFF5);

  static const Color notionBgPink = Color(0xFFFAF1F5);

  static const Color notionBgRed = Color(0xFFFBECEC);

  // ============================================================
  // NAVIGATION / SELECTION
  // ============================================================

  /// Selected background
  static const Color selectedBackground = Color(0xFFE8EFF4);

  /// Selected border
  static const Color selectedBorder = Color(0xFFD4E0E7);

  /// Selected foreground
  static const Color selectedForeground = Color(0xFF2F485C);

  // ============================================================
  // OVERLAY / SHADOW
  // ============================================================

  static const Color overlay = Color(0x14000000);

  static const Color shadow = Color(0x12000000);

  // ============================================================
  // ACCENT — TERRACOTTA
  // ============================================================

  static const Color accent = Color(0xFFC4775A);
  static const Color accentLight = Color(0xFFF6E9E4);
  static const Color accentDark = Color(0xFFA85F46);
}
